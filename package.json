{
  "name": "front-end-test-2",
  "version": "1.0.0",
  "description": "Simple Parcel Sandbox",
  "main": "index.html",
  "scripts": {
    "start": "parcel index.html --open",
    "test": "jest",
    "test:watch": "jest --config ./etc/jest/jest.unit.config.js --watch",
    "test:coverage": "jest --config ./etc/jest/jest.unit.config.js --coverage",
    "test:e2e": "jest --config ./etc/jest/jest.e2e.config.js",
    "test:e2e:watch": "jest --config ./etc/jest/jest.e2e.config.js --watch",
    "run:e2e:tests": "run-p start test:e2e:watch"
  },
  "babel": {
    "presets": [
      [
        "@babel/preset-env",
        {
          "targets": {
            "node": "current"
          }
        }
      ]
    ]
  },
  "dependencies": {
    "axios": "^0.21.1",
    "normalize.css": "^8.0.1",
    "parcel": "1.12.3"
  },
  "devDependencies": {
    "@babel/core": "^7.14.3",
    "@babel/preset-env": "^7.14.2",
    "babel-jest": "^27.0.1",
    "jest": "^27.0.1",
    "npm-run-all": "^4.1.5",
    "parcel-bundler": "^1.6.1",
    "puppeteer": "^9.1.1",
    "sass": "^1.33.0"
  },
  "keywords": []
}
